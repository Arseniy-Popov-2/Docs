Metaclasses
----------

``type`` is the default metaclass. Its signature is ``(name, bases, attrs)``,
where

    - ``name``: class name
    - ``bases``: tuple of the parent classes
    - ``namespace``: dictionary of attribute names and values.

It returns a class.

